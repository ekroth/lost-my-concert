# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           com.github.ekroth.controllers.UserInfo.index

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)

GET     /authorize                  com.github.ekroth.controllers.Authorization.authorize

GET     /authorized                 com.github.ekroth.controllers.Authorization.authorized

GET     /search/:name               com.github.ekroth.controllers.Kick.venues(name)
